{
  "name": "hrdle-setup",
  "version": "0.1.0",
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^8.0.1",
    "wrangler": "^4.58.0"
  },
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "deploy": "bun run build && wrangler deploy"
  },
  "type": "module"
}
